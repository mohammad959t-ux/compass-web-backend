# 🚀 تشغيل السيرفر مع VPN - دليل سريع

## الطريقة السريعة

### 1️⃣ اكتشف IP الحالي (مع VPN متصل):
```powershell
.\check-ip.ps1
```

### 2️⃣ أضف IP إلى MongoDB Atlas:
- افتح: https://cloud.mongodb.com/ → Security → Network Access
- اضغط "Add IP Address"
- أضف IP الذي رأيته في الخطوة 1
- أو اختر "Allow Access from Anywhere" (0.0.0.0/0) للاختبار

### 3️⃣ شغل السيرفر:
```powershell
.\start-with-vpn.ps1
```

---

## الحلول السريعة

### ✅ الحل الأفضل للتطوير:
**استخدم "Allow Access from Anywhere" (0.0.0.0/0)**
- يعمل مع أي VPN
- يعمل مع أي IP
- ⚠️ غير آمن للإنتاج - استخدم فقط للاختبار

### ✅ الحل للإنتاج:
**استخدم Static IP VPN**
- يحافظ على نفس IP دائماً
- أكثر أماناً
- يحتاج إضافة IP مرة واحدة فقط

---

## الأوامر المتاحة

### فحص IP:
```powershell
.\check-ip.ps1
```

### تشغيل السيرفر مع فحص اتصال:
```powershell
.\start-with-vpn.ps1
```

### تشغيل السيرفر مباشرة:
```powershell
cd compass_backend
npm run dev
```

### اختبار الاتصال فقط:
```powershell
node test-connection.js
```

---

## ملفات مساعدة

- 📄 `VPN_MONGODB_SETUP.md` - دليل شامل للحلول
- 🔧 `check-ip.ps1` - سكريبت فحص IP
- 🚀 `start-with-vpn.ps1` - سكريبت تشغيل السيرفر
- 🧪 `test-connection.js` - اختبار الاتصال

---

## نصائح مهمة

1. **عند تغيير VPN**: أعد فحص IP وأضفه إلى Atlas
2. **للإنتاج**: استخدم Static IP VPN
3. **للتطوير**: استخدم "Allow Access from Anywhere"
4. **الاختبار**: استخدم `test-connection.js` قبل تشغيل السيرفر
